1.老师，昨天的分页查询和增删改放在一起的话，查询默认从第一页开始的，这样无论是添加还是修改成功都会回到第一页，用户都很难确认自己的
增加、删除和修改操作是否正确或者是否执行，添加的话添加成功后可以让其重定向到分页的
最后一页("/pageQuery?pageNumber="+(int)Math.ceil(userDao.totalCount()/pageSize)),
可是如何实现修改/删除后页面留在当前页也就是怎么拿到并且使用用户点击跳转之后的pageNumber呢？直接从PageBean里面取的话好像每次都是0  -.-+;
  如果是添加/修改/删除时 添加弹窗功能提示用户的话，每次重启之后的第一次操作，会默认弹窗显示错误(数据却操作成功)，之后正常弹窗。来自小白的疑问，嘻嘻。
2.如何保证第一次增加或者修改时的布尔值不返回false,即在数据操作成功的同时保证弹窗的正确性;